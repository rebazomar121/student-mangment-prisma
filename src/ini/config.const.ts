const CONFIG_CONST_PORT = process.env.PORT || 1462 // default port to listen
const CONFIG_CONST_LISTEN_MESSAGE = "Express is listening at http://localhost:"

export const CONFIG_CONST = {
  CONFIG_CONST_PORT,
  CONFIG_CONST_LISTEN_MESSAGE,
}
